<!DOCTYPE html>
<html lang="en">
<head>
  <title>IOU<%= " - #{h @title}" if @title %></title>
</head>
<body>
  <header>
    <p><%= link_to "IOU", root_path %></p>
  </header>
  <%= yield %>
</body>
</html>
