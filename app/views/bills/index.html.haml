%h1= @title="Latest Bills"

%p= link_to "New", new_bill_path

%table
  %thead
    %tr
      %th Transaction
      %th Amount
      %th Description
      %th Date

  %tbody
    - for bill in @bills
      %tr
        %td
          %strong= h bill.from_user.name
          = h bill.past_verb
          %strong= h bill.to_user.name
        %td= h bill.amount.to_s + " €"
        %td= h bill.title
        %td= h bill.transaction_at
        %td= link_to "Show", bill


